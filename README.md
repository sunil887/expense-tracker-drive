# Expense Tracker (React Native + Google Drive)

This is the project you requested.  
To run:
1. Install Node + Expo CLI
2. Run `npm install`
3. Run `expo start`

Google Drive integration placeholders are included; you must add OAuth credentials.
